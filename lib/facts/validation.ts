import type { FactType } from "../../types/case";
import type { ExtractedFactCandidate } from "../ai/provider";
const supported=new Set<FactType>(["person","organization","order_id","product","amount","currency","date","delivery_date","purchase_date","complaint_date","refund_date","communication","status","location","deadline","promised_action","generic"]);
export function validateExtractedFact(candidate:ExtractedFactCandidate,context:{caseId:string;sourceId:string;sourceText?:string}){if(!supported.has(candidate.factType))throw new Error("The extracted fact type is not supported.");if(!candidate.value.trim())throw new Error("The extracted fact is empty.");if(!Number.isFinite(candidate.confidence)||candidate.confidence<0||candidate.confidence>1)throw new Error("The extracted confidence must be between 0 and 1.");if(candidate.factType.endsWith("date")||candidate.factType==="date"||candidate.factType==="deadline"){const normalized=candidate.normalizedValue??candidate.value;if(!/^\d{4}-\d{2}-\d{2}$/.test(normalized)||Number.isNaN(Date.parse(normalized)))throw new Error("The extracted date is not valid.");}if(candidate.factType==="amount"&&!/[0-9]/.test(candidate.value))throw new Error("The extracted amount is not valid.");if(!context.caseId||!context.sourceId)throw new Error("Fact provenance is incomplete.");return true;}
