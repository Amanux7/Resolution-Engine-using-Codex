"use client";
import { ArrowRight, BookOpen } from "lucide-react";
import { sampleCases, type SampleCaseId } from "@/data/demo/sample-cases";

export function SampleCaseLibrary({onOpen,loading}:{onOpen:(id:SampleCaseId)=>void;loading:boolean}){
  return <section aria-labelledby="sample-library" className="mt-10 border-t border-line pt-8"><div className="mb-5 flex items-end justify-between gap-4"><div><p className="editorial-label">Sample case library</p><h2 id="sample-library" className="editorial-heading mt-2 text-3xl text-ink">Different problems. One Resolution Engine.</h2></div><BookOpen className="hidden text-brand-600 sm:block" size={28} aria-hidden="true"/></div><p className="mb-6 max-w-2xl text-sm leading-6 text-muted">Explore fictional cases across documents, applications, bills, payments and consumer issues. Each follows the same evidence-first process.</p><div className="grid gap-px overflow-hidden border border-line bg-line sm:grid-cols-2">{sampleCases.map(sample=><article key={sample.id} className="bg-paper p-5"><p className="editorial-label text-brand-600">{sample.category}</p><h3 className="editorial-heading mt-2 text-xl text-ink">{sample.title}</h3><p className="mt-2 text-sm leading-6 text-muted">{sample.summary}</p><div className="mt-4 flex items-center justify-between gap-3 text-xs text-muted"><span>{sample.source.evidence.length} evidence items</span><span className="text-right">{sample.uncertainty}</span></div><button type="button" disabled={loading} onClick={()=>onOpen(sample.id)} className="mt-5 inline-flex min-h-11 items-center gap-2 text-sm font-semibold text-brand-700 underline underline-offset-4 disabled:opacity-60">Open sample case <ArrowRight size={16} aria-hidden="true"/></button></article>)}</div><p className="mt-4 text-xs text-muted">Sample cases are fictional. Nothing is sent or submitted.</p></section>;
}

