# Default local and test mode. No API key is required.
AI_MODE=mock

# Optional server-only multimodal image extraction.
# Set AI_MODE=openai only after adding a project key locally.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1-mini
