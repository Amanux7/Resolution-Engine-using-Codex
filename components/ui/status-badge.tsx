import { clsx } from "clsx";
const labels={found:"Supported by evidence",not_provided:"Not provided",needs_confirmation:"Needs checking",pending:"Adding your file",processing:"Reading your file",processed:"Ready to review",failed:"Couldn’t read",needs_processing:"Try again later",suggested:"Suggested",missing:"We still need this",unknown:"We don’t know yet",unverified:"Not confirmed"} as const;
export function StatusBadge({status}:{status:keyof typeof labels}){return <span className={clsx("inline-flex rounded-full px-2.5 py-1 text-xs font-semibold",(status==="found"||status==="processed"||status==="suggested")&&"bg-brand-50 text-brand-700",(status==="needs_confirmation"||status==="processing"||status==="pending"||status==="needs_processing"||status==="unverified")&&"bg-amber-50 text-amber-600",(status==="not_provided"||status==="unknown")&&"bg-canvas text-muted",(status==="failed"||status==="missing")&&"bg-danger-50 text-danger-600")}>{labels[status]}</span>;}
